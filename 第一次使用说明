1、创建环境
conda create -n r46（这个是环境名称） r-base=4.6（这个是r的版本根据使用情况设置） -c conda-forge

2、激活环境
conda activate r46（还是环境名称）

3、在目前所建立的环境里安装 Jupyter Notebook、JupyterLab 和客户端
conda install -c conda-forge notebook jupyterlab jupyter_client

4、进入R
在终端直接输入R然后回车进入R

5、然后运行（下面两个指令分别运行，运行完第一个，成功之后再运行第二个）
install.packages("IRkernel"）
IRkernel::installspec(name = "ir46", displayname = "R 4.6")（注意这里最好要写name是什么，以及displayname是什么，要不然后面容易搞混）

6、验证内核是否注册成功
先退出R
输入q()
再在终端运行
jupyter kernelspec list

7、测试 R 环境是否可用
sessionInfo()

